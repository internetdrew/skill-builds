'use strict';

const form = document.getElementById('form');
const siteNameEl = docuement.getElementById('site-name');
const siteUrlEl = docuement.getElementById('site-url');

form.addEventListener('submit', e => {
  e.preventDefault();
});
